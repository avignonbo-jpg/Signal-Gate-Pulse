<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Core telephony permissions -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_NUMBERS" />
    <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />
    <uses-permission android:name="android.permission.WRITE_CALL_LOG" />

    <!-- Notifications — POST_NOTIFICATIONS is all that is needed for Step 1.8.
         The overlay window permission has been intentionally omitted. SignalGate
         Pulse does not draw over other apps. Any PR reintroducing an overlay
         permission should be blocked — see Architecture Contract v5. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Network -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Contacts -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Foreground service (Layer 1 — platform boundary) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@drawable/shield_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/shield_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.SignalGate">

        <!-- Single Activity — contract requirement -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep link for blocked-call notification tap → Screen.Digest.
                 Internal scheme only — not registered for browser handling.
                 NavController resolves signalgate://digest automatically via
                 the navDeepLink declaration in NavGraph.kt. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data
                    android:scheme="signalgate"
                    android:host="digest" />
            </intent-filter>

        </activity>

        <!-- Call Screening Service (Layer 1 — system-level call decisions) -->
        <!-- BIND_SCREENING_SERVICE ensures only the system can bind to this service -->
        <service
            android:name=".SignalGateCallScreeningService"
            android:permission="android.permission.BIND_SCREENING_SERVICE"
            android:exported="true">

            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>

        </service>

        <!-- Call Overlay Service — class retained, not called from Step 1.8 onward.
             PULSE-TODO (2026-06-28): Remove this entry in the CallOverlayService
             cleanup PR once the class itself is deleted. -->
        <service
            android:name=".CallOverlayService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="phoneCall" />

        <!-- Phone state receiver — guards against third-party triggers -->
        <receiver
            android:name=".PhoneStateReceiver"
            android:permission="android.permission.READ_PHONE_STATE"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.PHONE_STATE" />
            </intent-filter>

        </receiver>

        <!-- Notification action buttons — internal only -->
        <receiver
            android:name=".CallActionReceiver"
            android:exported="false" />

    </application>

</manifest>
